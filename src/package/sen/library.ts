/**
 * `@seihouse/sen/library` — DEPRECATED alias for `@seihouse/sen/ui`.
 *
 * The primitives are SEN's own surface kit, not the Library application, so
 * this entry was renamed to `./ui` when SEN and Library split into separate
 * packages. The alias stays for one version so an in-flight integration keeps
 * building; import `@seihouse/sen/ui` instead.
 */
export * from './ui';

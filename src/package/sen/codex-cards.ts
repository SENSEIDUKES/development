/**
 * `@seihouse/sen/codex-cards` — DEPRECATED alias for `@seihouse/sen/cards`.
 *
 * The Codex cards were never a separate family: they are the narrative cards
 * of SEN's one shared card system. The alias stays for one version so an
 * in-flight integration keeps building; import `@seihouse/sen/cards` instead.
 */
export * from './cards';

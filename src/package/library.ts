/**
 * `@seihouse/sen/library` — the Celestial Library design system.
 *
 * The shared, portable primitives every SEN surface is skinned with:
 * panels, cards, buttons, the Manifest action, text inputs, navigation,
 * and the Library glyphs. This entry also carries the Library's own
 * stylesheets as a side effect, so a consumer that imports the primitives
 * gets the glass-field and spectrum treatments with them.
 */
export * from '../components/library';

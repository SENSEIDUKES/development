/**
 * The Celestial Library emblem, served from SEIHouse media so every host —
 * the Workshop, Light-Novels, or another SEN application — shows the same
 * mark without copying a file into its own public folder. Hosts can still
 * pass their own `emblem` where a surface accepts one.
 */
export const LIBRARY_EMBLEM = {
  src: 'https://media.seihouse.org/SEN/IMAGE/ICON/Header/CELESTIAL%20LIBRARY%20ICON.jpg',
  alt: 'Celestial Library Logo',
} as const;
